---
const { index, eyebrow, title, body } = Astro.props;
---
<div class="section-heading reveal">
  <div class="heading-meta"><span>{index}</span><span>{eyebrow}</span></div>
  <div class="heading-main">
    <h2 set:html={title}></h2>
    {body && <p>{body}</p>}
  </div>
</div>
